import type { DivMode } from "tsparticles/dist/Enums/Modes/DivMode";
import type { IOptionLoader } from "tsparticles/dist/Interfaces/Options/IOptionLoader";
export interface IDivEvent extends IOptionLoader<IDivEvent> {
    enable: boolean;
    mode: DivMode | DivMode[];
    el: string;
    elementId: string;
}
